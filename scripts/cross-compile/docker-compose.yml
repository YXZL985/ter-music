version: '3.8'

services:
  cross-build:
    build:
      context: .
      dockerfile: scripts/cross-compile/Dockerfile
    image: ter-music-cross:latest
    container_name: ter-music-cross
    volumes:
      - .:/workspace
    working_dir: /workspace
    # Override command as needed
    command: ["./scripts/build/build-deb.sh", "-a", "arm64"]
    # Keep container running for interactive use
    stdin_open: true
    tty: true
